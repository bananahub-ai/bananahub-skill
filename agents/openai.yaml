interface:
  display_name: "BananaHub"
  short_description: "Provider-aware image generation, editing, host tools, and templates"
  default_prompt: "Use $bananahub to optimize a non-English or mixed-language image request, generate or edit through GPT Image, Gemini, a compatible gateway, or a host-native image tool, and discover matching templates."

policy:
  allow_implicit_invocation: false
